import React from 'react'
import ReactDom from 'react-dom'
import Root from './containers/Root'
import './index.css'

// Rendering our Root component in #root HTML element
ReactDom.render(<Root />, document.getElementById('root'))
